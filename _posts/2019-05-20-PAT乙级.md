---
layout:     post
title:      PAT乙级(Basic Level)真题
subtitle:   Java解题记录
date:       2018-05-20
author:     Alitria
header-img: img/post-bg-ios9-web.jpg
catalog: true
tags:
    - PAT乙级
    - Java
    - 总结
    - 牛客网
---

## A+B和C (15)
题目描述
给定区间[-2的31次方, 2的31次方]内的3个整数A、B和C，请判断A+B是否大于C。
```
import org.omg.PortableInterceptor.INACTIVE;

import java.util.*;
import java.io.*;
import java.math.*;
public class Main{

    public static void main(String[] args) throws Exception{
        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in));
        int size = Integer.parseInt(bufferedReader.readLine());
        int index = 1;
        while(size>0) {
            String[] data = bufferedReader.readLine().split(" ");
            double a = Double.parseDouble(data[0]);
            double b = Double.parseDouble(data[1]);
            double c = Double.parseDouble(data[2]);
            if(a+b>c)
                System.out.println("Case #"+index+": "+"true");
            else
                System.out.println("Case #"+index+": "+"false");
            index++;
            size--;
        }
    }

}
```

## 数素数 (20)
令Pi表示第i个素数。现任给两个正整数M <= N <= 10000，请输出PM到PN的所有素数。
```
import org.omg.PortableInterceptor.INACTIVE;

import java.util.*;
import java.io.*;
import java.math.*;
public class Main{

    public static void main(String[] args) throws Exception{
        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in));
        String[] data = bufferedReader.readLine().split(" ");
        int m = Integer.parseInt(data[0]);
        int n = Integer.parseInt(data[1]);
        boolean[] tmp = new boolean[104730];
        for(int i=2; i<=104729; i++) {
            int factor = 2;
            for(int j=i*factor; j<=104729; j=i*factor) {
                tmp[j] = true;
                factor++;
            }
        }
        Vector<Integer> vector = new Vector<>();
        int index = 0;
        for(int i=2; i<=104729; i++) {
            if(tmp[i]==false) {
                index++;
                if(index>=m && index<=n)
                    vector.add(i);
            }
        }
        index = 0;
        for(int i=0; i<vector.size(); i++) {
            index++;
            if(i==vector.size()-1) {
                System.out.print(vector.get(i));
                System.out.println();
                break;
            }
            if(index<10) {
                System.out.print(vector.get(i));
                System.out.print(" ");
            }
            else {
                System.out.print(vector.get(i));
                System.out.println();
                index = 0;
            }
        }
    }

}
```

## 部分A+B (15)  
正整数A的“DA（为1位整数）部分”定义为由A中所有DA组成的新整数PA。例如：给定A = 3862767，DA = 6，则A的“6部分”PA是66，因为A中有2个6。现给定A、DA、B、DB，请编写程序计算PA + PB。 
```
import org.omg.PortableInterceptor.INACTIVE;

import java.util.*;
import java.io.*;
import java.math.*;
public class Main{

    public static void main(String[] args) throws Exception{
        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in));
        String[] data = bufferedReader.readLine().split(" ");
        long a = Long.parseLong(data[0]);
        long da = Long.parseLong(data[1]);
        long b = Long.parseLong(data[2]);
        long db = Long.parseLong(data[3]);
        long apk = getNewNumber(a, da) + getNewNumber(b, db);
        System.out.println(apk);
    }

    public static long getNewNumber(long data, long factor) {
        long ans = 0;
        while(data>0) {
            if(data%10==factor) {
                ans = ans*10 + factor;
            }
            data /= 10;
        }
        return ans;
    }

}
```
